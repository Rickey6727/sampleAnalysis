package com.example.sample.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TBlockAccount89Listener implements EntityListener<TBlockAccount89> {

    @Override
    public void preInsert(TBlockAccount89 entity, PreInsertContext<TBlockAccount89> context) {
    }

    @Override
    public void preUpdate(TBlockAccount89 entity, PreUpdateContext<TBlockAccount89> context) {
    }

    @Override
    public void preDelete(TBlockAccount89 entity, PreDeleteContext<TBlockAccount89> context) {
    }

    @Override
    public void postInsert(TBlockAccount89 entity, PostInsertContext<TBlockAccount89> context) {
    }

    @Override
    public void postUpdate(TBlockAccount89 entity, PostUpdateContext<TBlockAccount89> context) {
    }

    @Override
    public void postDelete(TBlockAccount89 entity, PostDeleteContext<TBlockAccount89> context) {
    }
}