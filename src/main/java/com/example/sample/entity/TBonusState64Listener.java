package com.example.sample.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TBonusState64Listener implements EntityListener<TBonusState64> {

    @Override
    public void preInsert(TBonusState64 entity, PreInsertContext<TBonusState64> context) {
    }

    @Override
    public void preUpdate(TBonusState64 entity, PreUpdateContext<TBonusState64> context) {
    }

    @Override
    public void preDelete(TBonusState64 entity, PreDeleteContext<TBonusState64> context) {
    }

    @Override
    public void postInsert(TBonusState64 entity, PostInsertContext<TBonusState64> context) {
    }

    @Override
    public void postUpdate(TBonusState64 entity, PostUpdateContext<TBonusState64> context) {
    }

    @Override
    public void postDelete(TBonusState64 entity, PostDeleteContext<TBonusState64> context) {
    }
}