package com.example.sample.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TBlockAccount84Listener implements EntityListener<TBlockAccount84> {

    @Override
    public void preInsert(TBlockAccount84 entity, PreInsertContext<TBlockAccount84> context) {
    }

    @Override
    public void preUpdate(TBlockAccount84 entity, PreUpdateContext<TBlockAccount84> context) {
    }

    @Override
    public void preDelete(TBlockAccount84 entity, PreDeleteContext<TBlockAccount84> context) {
    }

    @Override
    public void postInsert(TBlockAccount84 entity, PostInsertContext<TBlockAccount84> context) {
    }

    @Override
    public void postUpdate(TBlockAccount84 entity, PostUpdateContext<TBlockAccount84> context) {
    }

    @Override
    public void postDelete(TBlockAccount84 entity, PostDeleteContext<TBlockAccount84> context) {
    }
}