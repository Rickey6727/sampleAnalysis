package com.example.sample.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TBlockAccount71Listener implements EntityListener<TBlockAccount71> {

    @Override
    public void preInsert(TBlockAccount71 entity, PreInsertContext<TBlockAccount71> context) {
    }

    @Override
    public void preUpdate(TBlockAccount71 entity, PreUpdateContext<TBlockAccount71> context) {
    }

    @Override
    public void preDelete(TBlockAccount71 entity, PreDeleteContext<TBlockAccount71> context) {
    }

    @Override
    public void postInsert(TBlockAccount71 entity, PostInsertContext<TBlockAccount71> context) {
    }

    @Override
    public void postUpdate(TBlockAccount71 entity, PostUpdateContext<TBlockAccount71> context) {
    }

    @Override
    public void postDelete(TBlockAccount71 entity, PostDeleteContext<TBlockAccount71> context) {
    }
}