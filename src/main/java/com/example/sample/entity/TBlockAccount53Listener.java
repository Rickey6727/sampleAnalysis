package com.example.sample.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TBlockAccount53Listener implements EntityListener<TBlockAccount53> {

    @Override
    public void preInsert(TBlockAccount53 entity, PreInsertContext<TBlockAccount53> context) {
    }

    @Override
    public void preUpdate(TBlockAccount53 entity, PreUpdateContext<TBlockAccount53> context) {
    }

    @Override
    public void preDelete(TBlockAccount53 entity, PreDeleteContext<TBlockAccount53> context) {
    }

    @Override
    public void postInsert(TBlockAccount53 entity, PostInsertContext<TBlockAccount53> context) {
    }

    @Override
    public void postUpdate(TBlockAccount53 entity, PostUpdateContext<TBlockAccount53> context) {
    }

    @Override
    public void postDelete(TBlockAccount53 entity, PostDeleteContext<TBlockAccount53> context) {
    }
}