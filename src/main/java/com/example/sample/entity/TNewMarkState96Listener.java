package com.example.sample.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TNewMarkState96Listener implements EntityListener<TNewMarkState96> {

    @Override
    public void preInsert(TNewMarkState96 entity, PreInsertContext<TNewMarkState96> context) {
    }

    @Override
    public void preUpdate(TNewMarkState96 entity, PreUpdateContext<TNewMarkState96> context) {
    }

    @Override
    public void preDelete(TNewMarkState96 entity, PreDeleteContext<TNewMarkState96> context) {
    }

    @Override
    public void postInsert(TNewMarkState96 entity, PostInsertContext<TNewMarkState96> context) {
    }

    @Override
    public void postUpdate(TNewMarkState96 entity, PostUpdateContext<TNewMarkState96> context) {
    }

    @Override
    public void postDelete(TNewMarkState96 entity, PostDeleteContext<TNewMarkState96> context) {
    }
}