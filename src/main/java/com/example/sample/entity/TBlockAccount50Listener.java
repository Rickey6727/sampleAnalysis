package com.example.sample.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TBlockAccount50Listener implements EntityListener<TBlockAccount50> {

    @Override
    public void preInsert(TBlockAccount50 entity, PreInsertContext<TBlockAccount50> context) {
    }

    @Override
    public void preUpdate(TBlockAccount50 entity, PreUpdateContext<TBlockAccount50> context) {
    }

    @Override
    public void preDelete(TBlockAccount50 entity, PreDeleteContext<TBlockAccount50> context) {
    }

    @Override
    public void postInsert(TBlockAccount50 entity, PostInsertContext<TBlockAccount50> context) {
    }

    @Override
    public void postUpdate(TBlockAccount50 entity, PostUpdateContext<TBlockAccount50> context) {
    }

    @Override
    public void postDelete(TBlockAccount50 entity, PostDeleteContext<TBlockAccount50> context) {
    }
}