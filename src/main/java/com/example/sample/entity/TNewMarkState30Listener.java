package com.example.sample.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TNewMarkState30Listener implements EntityListener<TNewMarkState30> {

    @Override
    public void preInsert(TNewMarkState30 entity, PreInsertContext<TNewMarkState30> context) {
    }

    @Override
    public void preUpdate(TNewMarkState30 entity, PreUpdateContext<TNewMarkState30> context) {
    }

    @Override
    public void preDelete(TNewMarkState30 entity, PreDeleteContext<TNewMarkState30> context) {
    }

    @Override
    public void postInsert(TNewMarkState30 entity, PostInsertContext<TNewMarkState30> context) {
    }

    @Override
    public void postUpdate(TNewMarkState30 entity, PostUpdateContext<TNewMarkState30> context) {
    }

    @Override
    public void postDelete(TNewMarkState30 entity, PostDeleteContext<TNewMarkState30> context) {
    }
}