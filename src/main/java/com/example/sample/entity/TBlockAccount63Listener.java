package com.example.sample.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TBlockAccount63Listener implements EntityListener<TBlockAccount63> {

    @Override
    public void preInsert(TBlockAccount63 entity, PreInsertContext<TBlockAccount63> context) {
    }

    @Override
    public void preUpdate(TBlockAccount63 entity, PreUpdateContext<TBlockAccount63> context) {
    }

    @Override
    public void preDelete(TBlockAccount63 entity, PreDeleteContext<TBlockAccount63> context) {
    }

    @Override
    public void postInsert(TBlockAccount63 entity, PostInsertContext<TBlockAccount63> context) {
    }

    @Override
    public void postUpdate(TBlockAccount63 entity, PostUpdateContext<TBlockAccount63> context) {
    }

    @Override
    public void postDelete(TBlockAccount63 entity, PostDeleteContext<TBlockAccount63> context) {
    }
}