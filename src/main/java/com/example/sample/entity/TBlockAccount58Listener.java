package com.example.sample.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TBlockAccount58Listener implements EntityListener<TBlockAccount58> {

    @Override
    public void preInsert(TBlockAccount58 entity, PreInsertContext<TBlockAccount58> context) {
    }

    @Override
    public void preUpdate(TBlockAccount58 entity, PreUpdateContext<TBlockAccount58> context) {
    }

    @Override
    public void preDelete(TBlockAccount58 entity, PreDeleteContext<TBlockAccount58> context) {
    }

    @Override
    public void postInsert(TBlockAccount58 entity, PostInsertContext<TBlockAccount58> context) {
    }

    @Override
    public void postUpdate(TBlockAccount58 entity, PostUpdateContext<TBlockAccount58> context) {
    }

    @Override
    public void postDelete(TBlockAccount58 entity, PostDeleteContext<TBlockAccount58> context) {
    }
}