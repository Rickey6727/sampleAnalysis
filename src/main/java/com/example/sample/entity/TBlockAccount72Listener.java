package com.example.sample.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TBlockAccount72Listener implements EntityListener<TBlockAccount72> {

    @Override
    public void preInsert(TBlockAccount72 entity, PreInsertContext<TBlockAccount72> context) {
    }

    @Override
    public void preUpdate(TBlockAccount72 entity, PreUpdateContext<TBlockAccount72> context) {
    }

    @Override
    public void preDelete(TBlockAccount72 entity, PreDeleteContext<TBlockAccount72> context) {
    }

    @Override
    public void postInsert(TBlockAccount72 entity, PostInsertContext<TBlockAccount72> context) {
    }

    @Override
    public void postUpdate(TBlockAccount72 entity, PostUpdateContext<TBlockAccount72> context) {
    }

    @Override
    public void postDelete(TBlockAccount72 entity, PostDeleteContext<TBlockAccount72> context) {
    }
}