package com.example.sample.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TNewMarkState65Listener implements EntityListener<TNewMarkState65> {

    @Override
    public void preInsert(TNewMarkState65 entity, PreInsertContext<TNewMarkState65> context) {
    }

    @Override
    public void preUpdate(TNewMarkState65 entity, PreUpdateContext<TNewMarkState65> context) {
    }

    @Override
    public void preDelete(TNewMarkState65 entity, PreDeleteContext<TNewMarkState65> context) {
    }

    @Override
    public void postInsert(TNewMarkState65 entity, PostInsertContext<TNewMarkState65> context) {
    }

    @Override
    public void postUpdate(TNewMarkState65 entity, PostUpdateContext<TNewMarkState65> context) {
    }

    @Override
    public void postDelete(TNewMarkState65 entity, PostDeleteContext<TNewMarkState65> context) {
    }
}