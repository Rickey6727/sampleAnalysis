package com.example.sample.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TBlockAccount73Listener implements EntityListener<TBlockAccount73> {

    @Override
    public void preInsert(TBlockAccount73 entity, PreInsertContext<TBlockAccount73> context) {
    }

    @Override
    public void preUpdate(TBlockAccount73 entity, PreUpdateContext<TBlockAccount73> context) {
    }

    @Override
    public void preDelete(TBlockAccount73 entity, PreDeleteContext<TBlockAccount73> context) {
    }

    @Override
    public void postInsert(TBlockAccount73 entity, PostInsertContext<TBlockAccount73> context) {
    }

    @Override
    public void postUpdate(TBlockAccount73 entity, PostUpdateContext<TBlockAccount73> context) {
    }

    @Override
    public void postDelete(TBlockAccount73 entity, PostDeleteContext<TBlockAccount73> context) {
    }
}