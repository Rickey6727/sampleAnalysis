package com.example.sample.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TNotice78Listener implements EntityListener<TNotice78> {

    @Override
    public void preInsert(TNotice78 entity, PreInsertContext<TNotice78> context) {
    }

    @Override
    public void preUpdate(TNotice78 entity, PreUpdateContext<TNotice78> context) {
    }

    @Override
    public void preDelete(TNotice78 entity, PreDeleteContext<TNotice78> context) {
    }

    @Override
    public void postInsert(TNotice78 entity, PostInsertContext<TNotice78> context) {
    }

    @Override
    public void postUpdate(TNotice78 entity, PostUpdateContext<TNotice78> context) {
    }

    @Override
    public void postDelete(TNotice78 entity, PostDeleteContext<TNotice78> context) {
    }
}