package com.example.sample.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TBlockAccount92Listener implements EntityListener<TBlockAccount92> {

    @Override
    public void preInsert(TBlockAccount92 entity, PreInsertContext<TBlockAccount92> context) {
    }

    @Override
    public void preUpdate(TBlockAccount92 entity, PreUpdateContext<TBlockAccount92> context) {
    }

    @Override
    public void preDelete(TBlockAccount92 entity, PreDeleteContext<TBlockAccount92> context) {
    }

    @Override
    public void postInsert(TBlockAccount92 entity, PostInsertContext<TBlockAccount92> context) {
    }

    @Override
    public void postUpdate(TBlockAccount92 entity, PostUpdateContext<TBlockAccount92> context) {
    }

    @Override
    public void postDelete(TBlockAccount92 entity, PostDeleteContext<TBlockAccount92> context) {
    }
}