package com.example.sample.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TNewMarkState86Listener implements EntityListener<TNewMarkState86> {

    @Override
    public void preInsert(TNewMarkState86 entity, PreInsertContext<TNewMarkState86> context) {
    }

    @Override
    public void preUpdate(TNewMarkState86 entity, PreUpdateContext<TNewMarkState86> context) {
    }

    @Override
    public void preDelete(TNewMarkState86 entity, PreDeleteContext<TNewMarkState86> context) {
    }

    @Override
    public void postInsert(TNewMarkState86 entity, PostInsertContext<TNewMarkState86> context) {
    }

    @Override
    public void postUpdate(TNewMarkState86 entity, PostUpdateContext<TNewMarkState86> context) {
    }

    @Override
    public void postDelete(TNewMarkState86 entity, PostDeleteContext<TNewMarkState86> context) {
    }
}