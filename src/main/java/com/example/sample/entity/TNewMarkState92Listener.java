package com.example.sample.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TNewMarkState92Listener implements EntityListener<TNewMarkState92> {

    @Override
    public void preInsert(TNewMarkState92 entity, PreInsertContext<TNewMarkState92> context) {
    }

    @Override
    public void preUpdate(TNewMarkState92 entity, PreUpdateContext<TNewMarkState92> context) {
    }

    @Override
    public void preDelete(TNewMarkState92 entity, PreDeleteContext<TNewMarkState92> context) {
    }

    @Override
    public void postInsert(TNewMarkState92 entity, PostInsertContext<TNewMarkState92> context) {
    }

    @Override
    public void postUpdate(TNewMarkState92 entity, PostUpdateContext<TNewMarkState92> context) {
    }

    @Override
    public void postDelete(TNewMarkState92 entity, PostDeleteContext<TNewMarkState92> context) {
    }
}