package com.example.sample.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TPresentBox64Listener implements EntityListener<TPresentBox64> {

    @Override
    public void preInsert(TPresentBox64 entity, PreInsertContext<TPresentBox64> context) {
    }

    @Override
    public void preUpdate(TPresentBox64 entity, PreUpdateContext<TPresentBox64> context) {
    }

    @Override
    public void preDelete(TPresentBox64 entity, PreDeleteContext<TPresentBox64> context) {
    }

    @Override
    public void postInsert(TPresentBox64 entity, PostInsertContext<TPresentBox64> context) {
    }

    @Override
    public void postUpdate(TPresentBox64 entity, PostUpdateContext<TPresentBox64> context) {
    }

    @Override
    public void postDelete(TPresentBox64 entity, PostDeleteContext<TPresentBox64> context) {
    }
}