package com.example.sample.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TBlockAccount86Listener implements EntityListener<TBlockAccount86> {

    @Override
    public void preInsert(TBlockAccount86 entity, PreInsertContext<TBlockAccount86> context) {
    }

    @Override
    public void preUpdate(TBlockAccount86 entity, PreUpdateContext<TBlockAccount86> context) {
    }

    @Override
    public void preDelete(TBlockAccount86 entity, PreDeleteContext<TBlockAccount86> context) {
    }

    @Override
    public void postInsert(TBlockAccount86 entity, PostInsertContext<TBlockAccount86> context) {
    }

    @Override
    public void postUpdate(TBlockAccount86 entity, PostUpdateContext<TBlockAccount86> context) {
    }

    @Override
    public void postDelete(TBlockAccount86 entity, PostDeleteContext<TBlockAccount86> context) {
    }
}