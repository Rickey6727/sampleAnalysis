package com.example.sample.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TBlockAccount60Listener implements EntityListener<TBlockAccount60> {

    @Override
    public void preInsert(TBlockAccount60 entity, PreInsertContext<TBlockAccount60> context) {
    }

    @Override
    public void preUpdate(TBlockAccount60 entity, PreUpdateContext<TBlockAccount60> context) {
    }

    @Override
    public void preDelete(TBlockAccount60 entity, PreDeleteContext<TBlockAccount60> context) {
    }

    @Override
    public void postInsert(TBlockAccount60 entity, PostInsertContext<TBlockAccount60> context) {
    }

    @Override
    public void postUpdate(TBlockAccount60 entity, PostUpdateContext<TBlockAccount60> context) {
    }

    @Override
    public void postDelete(TBlockAccount60 entity, PostDeleteContext<TBlockAccount60> context) {
    }
}