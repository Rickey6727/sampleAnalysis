package com.example.sample.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TBlockAccount91Listener implements EntityListener<TBlockAccount91> {

    @Override
    public void preInsert(TBlockAccount91 entity, PreInsertContext<TBlockAccount91> context) {
    }

    @Override
    public void preUpdate(TBlockAccount91 entity, PreUpdateContext<TBlockAccount91> context) {
    }

    @Override
    public void preDelete(TBlockAccount91 entity, PreDeleteContext<TBlockAccount91> context) {
    }

    @Override
    public void postInsert(TBlockAccount91 entity, PostInsertContext<TBlockAccount91> context) {
    }

    @Override
    public void postUpdate(TBlockAccount91 entity, PostUpdateContext<TBlockAccount91> context) {
    }

    @Override
    public void postDelete(TBlockAccount91 entity, PostDeleteContext<TBlockAccount91> context) {
    }
}