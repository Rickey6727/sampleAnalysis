package com.example.sample.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TBlockAccount74Listener implements EntityListener<TBlockAccount74> {

    @Override
    public void preInsert(TBlockAccount74 entity, PreInsertContext<TBlockAccount74> context) {
    }

    @Override
    public void preUpdate(TBlockAccount74 entity, PreUpdateContext<TBlockAccount74> context) {
    }

    @Override
    public void preDelete(TBlockAccount74 entity, PreDeleteContext<TBlockAccount74> context) {
    }

    @Override
    public void postInsert(TBlockAccount74 entity, PostInsertContext<TBlockAccount74> context) {
    }

    @Override
    public void postUpdate(TBlockAccount74 entity, PostUpdateContext<TBlockAccount74> context) {
    }

    @Override
    public void postDelete(TBlockAccount74 entity, PostDeleteContext<TBlockAccount74> context) {
    }
}