package com.example.sample.entity.sales;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TGachaCollection61Listener implements EntityListener<TGachaCollection61> {

    @Override
    public void preInsert(TGachaCollection61 entity, PreInsertContext<TGachaCollection61> context) {
    }

    @Override
    public void preUpdate(TGachaCollection61 entity, PreUpdateContext<TGachaCollection61> context) {
    }

    @Override
    public void preDelete(TGachaCollection61 entity, PreDeleteContext<TGachaCollection61> context) {
    }

    @Override
    public void postInsert(TGachaCollection61 entity, PostInsertContext<TGachaCollection61> context) {
    }

    @Override
    public void postUpdate(TGachaCollection61 entity, PostUpdateContext<TGachaCollection61> context) {
    }

    @Override
    public void postDelete(TGachaCollection61 entity, PostDeleteContext<TGachaCollection61> context) {
    }
}