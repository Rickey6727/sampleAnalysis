package com.example.sample.entity.sales;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TGachaCollection60Listener implements EntityListener<TGachaCollection60> {

    @Override
    public void preInsert(TGachaCollection60 entity, PreInsertContext<TGachaCollection60> context) {
    }

    @Override
    public void preUpdate(TGachaCollection60 entity, PreUpdateContext<TGachaCollection60> context) {
    }

    @Override
    public void preDelete(TGachaCollection60 entity, PreDeleteContext<TGachaCollection60> context) {
    }

    @Override
    public void postInsert(TGachaCollection60 entity, PostInsertContext<TGachaCollection60> context) {
    }

    @Override
    public void postUpdate(TGachaCollection60 entity, PostUpdateContext<TGachaCollection60> context) {
    }

    @Override
    public void postDelete(TGachaCollection60 entity, PostDeleteContext<TGachaCollection60> context) {
    }
}