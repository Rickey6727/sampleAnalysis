package com.example.sample.entity.sales;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TGachaCollection34Listener implements EntityListener<TGachaCollection34> {

    @Override
    public void preInsert(TGachaCollection34 entity, PreInsertContext<TGachaCollection34> context) {
    }

    @Override
    public void preUpdate(TGachaCollection34 entity, PreUpdateContext<TGachaCollection34> context) {
    }

    @Override
    public void preDelete(TGachaCollection34 entity, PreDeleteContext<TGachaCollection34> context) {
    }

    @Override
    public void postInsert(TGachaCollection34 entity, PostInsertContext<TGachaCollection34> context) {
    }

    @Override
    public void postUpdate(TGachaCollection34 entity, PostUpdateContext<TGachaCollection34> context) {
    }

    @Override
    public void postDelete(TGachaCollection34 entity, PostDeleteContext<TGachaCollection34> context) {
    }
}