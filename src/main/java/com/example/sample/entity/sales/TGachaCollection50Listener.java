package com.example.sample.entity.sales;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TGachaCollection50Listener implements EntityListener<TGachaCollection50> {

    @Override
    public void preInsert(TGachaCollection50 entity, PreInsertContext<TGachaCollection50> context) {
    }

    @Override
    public void preUpdate(TGachaCollection50 entity, PreUpdateContext<TGachaCollection50> context) {
    }

    @Override
    public void preDelete(TGachaCollection50 entity, PreDeleteContext<TGachaCollection50> context) {
    }

    @Override
    public void postInsert(TGachaCollection50 entity, PostInsertContext<TGachaCollection50> context) {
    }

    @Override
    public void postUpdate(TGachaCollection50 entity, PostUpdateContext<TGachaCollection50> context) {
    }

    @Override
    public void postDelete(TGachaCollection50 entity, PostDeleteContext<TGachaCollection50> context) {
    }
}