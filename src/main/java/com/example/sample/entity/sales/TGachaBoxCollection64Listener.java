package com.example.sample.entity.sales;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TGachaBoxCollection64Listener implements EntityListener<TGachaBoxCollection64> {

    @Override
    public void preInsert(TGachaBoxCollection64 entity, PreInsertContext<TGachaBoxCollection64> context) {
    }

    @Override
    public void preUpdate(TGachaBoxCollection64 entity, PreUpdateContext<TGachaBoxCollection64> context) {
    }

    @Override
    public void preDelete(TGachaBoxCollection64 entity, PreDeleteContext<TGachaBoxCollection64> context) {
    }

    @Override
    public void postInsert(TGachaBoxCollection64 entity, PostInsertContext<TGachaBoxCollection64> context) {
    }

    @Override
    public void postUpdate(TGachaBoxCollection64 entity, PostUpdateContext<TGachaBoxCollection64> context) {
    }

    @Override
    public void postDelete(TGachaBoxCollection64 entity, PostDeleteContext<TGachaBoxCollection64> context) {
    }
}