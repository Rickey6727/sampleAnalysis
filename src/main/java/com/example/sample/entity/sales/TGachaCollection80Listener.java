package com.example.sample.entity.sales;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TGachaCollection80Listener implements EntityListener<TGachaCollection80> {

    @Override
    public void preInsert(TGachaCollection80 entity, PreInsertContext<TGachaCollection80> context) {
    }

    @Override
    public void preUpdate(TGachaCollection80 entity, PreUpdateContext<TGachaCollection80> context) {
    }

    @Override
    public void preDelete(TGachaCollection80 entity, PreDeleteContext<TGachaCollection80> context) {
    }

    @Override
    public void postInsert(TGachaCollection80 entity, PostInsertContext<TGachaCollection80> context) {
    }

    @Override
    public void postUpdate(TGachaCollection80 entity, PostUpdateContext<TGachaCollection80> context) {
    }

    @Override
    public void postDelete(TGachaCollection80 entity, PostDeleteContext<TGachaCollection80> context) {
    }
}