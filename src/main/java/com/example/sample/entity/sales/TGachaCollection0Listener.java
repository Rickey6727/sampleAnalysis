package com.example.sample.entity.sales;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TGachaCollection0Listener implements EntityListener<TGachaCollection0> {

    @Override
    public void preInsert(TGachaCollection0 entity, PreInsertContext<TGachaCollection0> context) {
    }

    @Override
    public void preUpdate(TGachaCollection0 entity, PreUpdateContext<TGachaCollection0> context) {
    }

    @Override
    public void preDelete(TGachaCollection0 entity, PreDeleteContext<TGachaCollection0> context) {
    }

    @Override
    public void postInsert(TGachaCollection0 entity, PostInsertContext<TGachaCollection0> context) {
    }

    @Override
    public void postUpdate(TGachaCollection0 entity, PostUpdateContext<TGachaCollection0> context) {
    }

    @Override
    public void postDelete(TGachaCollection0 entity, PostDeleteContext<TGachaCollection0> context) {
    }
}