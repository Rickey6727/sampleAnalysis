package com.example.sample.entity.sales;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TGachaCollection51Listener implements EntityListener<TGachaCollection51> {

    @Override
    public void preInsert(TGachaCollection51 entity, PreInsertContext<TGachaCollection51> context) {
    }

    @Override
    public void preUpdate(TGachaCollection51 entity, PreUpdateContext<TGachaCollection51> context) {
    }

    @Override
    public void preDelete(TGachaCollection51 entity, PreDeleteContext<TGachaCollection51> context) {
    }

    @Override
    public void postInsert(TGachaCollection51 entity, PostInsertContext<TGachaCollection51> context) {
    }

    @Override
    public void postUpdate(TGachaCollection51 entity, PostUpdateContext<TGachaCollection51> context) {
    }

    @Override
    public void postDelete(TGachaCollection51 entity, PostDeleteContext<TGachaCollection51> context) {
    }
}