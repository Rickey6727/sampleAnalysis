package com.example.sample.entity.sales;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TGachaCollection15Listener implements EntityListener<TGachaCollection15> {

    @Override
    public void preInsert(TGachaCollection15 entity, PreInsertContext<TGachaCollection15> context) {
    }

    @Override
    public void preUpdate(TGachaCollection15 entity, PreUpdateContext<TGachaCollection15> context) {
    }

    @Override
    public void preDelete(TGachaCollection15 entity, PreDeleteContext<TGachaCollection15> context) {
    }

    @Override
    public void postInsert(TGachaCollection15 entity, PostInsertContext<TGachaCollection15> context) {
    }

    @Override
    public void postUpdate(TGachaCollection15 entity, PostUpdateContext<TGachaCollection15> context) {
    }

    @Override
    public void postDelete(TGachaCollection15 entity, PostDeleteContext<TGachaCollection15> context) {
    }
}