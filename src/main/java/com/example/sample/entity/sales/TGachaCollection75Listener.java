package com.example.sample.entity.sales;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TGachaCollection75Listener implements EntityListener<TGachaCollection75> {

    @Override
    public void preInsert(TGachaCollection75 entity, PreInsertContext<TGachaCollection75> context) {
    }

    @Override
    public void preUpdate(TGachaCollection75 entity, PreUpdateContext<TGachaCollection75> context) {
    }

    @Override
    public void preDelete(TGachaCollection75 entity, PreDeleteContext<TGachaCollection75> context) {
    }

    @Override
    public void postInsert(TGachaCollection75 entity, PostInsertContext<TGachaCollection75> context) {
    }

    @Override
    public void postUpdate(TGachaCollection75 entity, PostUpdateContext<TGachaCollection75> context) {
    }

    @Override
    public void postDelete(TGachaCollection75 entity, PostDeleteContext<TGachaCollection75> context) {
    }
}