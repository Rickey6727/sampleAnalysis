package com.example.sample.entity.sales;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TGachaCollection47Listener implements EntityListener<TGachaCollection47> {

    @Override
    public void preInsert(TGachaCollection47 entity, PreInsertContext<TGachaCollection47> context) {
    }

    @Override
    public void preUpdate(TGachaCollection47 entity, PreUpdateContext<TGachaCollection47> context) {
    }

    @Override
    public void preDelete(TGachaCollection47 entity, PreDeleteContext<TGachaCollection47> context) {
    }

    @Override
    public void postInsert(TGachaCollection47 entity, PostInsertContext<TGachaCollection47> context) {
    }

    @Override
    public void postUpdate(TGachaCollection47 entity, PostUpdateContext<TGachaCollection47> context) {
    }

    @Override
    public void postDelete(TGachaCollection47 entity, PostDeleteContext<TGachaCollection47> context) {
    }
}