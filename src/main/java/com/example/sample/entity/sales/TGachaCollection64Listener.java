package com.example.sample.entity.sales;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TGachaCollection64Listener implements EntityListener<TGachaCollection64> {

    @Override
    public void preInsert(TGachaCollection64 entity, PreInsertContext<TGachaCollection64> context) {
    }

    @Override
    public void preUpdate(TGachaCollection64 entity, PreUpdateContext<TGachaCollection64> context) {
    }

    @Override
    public void preDelete(TGachaCollection64 entity, PreDeleteContext<TGachaCollection64> context) {
    }

    @Override
    public void postInsert(TGachaCollection64 entity, PostInsertContext<TGachaCollection64> context) {
    }

    @Override
    public void postUpdate(TGachaCollection64 entity, PostUpdateContext<TGachaCollection64> context) {
    }

    @Override
    public void postDelete(TGachaCollection64 entity, PostDeleteContext<TGachaCollection64> context) {
    }
}