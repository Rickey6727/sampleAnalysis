package com.example.sample.entity.sales;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TGachaCollection8Listener implements EntityListener<TGachaCollection8> {

    @Override
    public void preInsert(TGachaCollection8 entity, PreInsertContext<TGachaCollection8> context) {
    }

    @Override
    public void preUpdate(TGachaCollection8 entity, PreUpdateContext<TGachaCollection8> context) {
    }

    @Override
    public void preDelete(TGachaCollection8 entity, PreDeleteContext<TGachaCollection8> context) {
    }

    @Override
    public void postInsert(TGachaCollection8 entity, PostInsertContext<TGachaCollection8> context) {
    }

    @Override
    public void postUpdate(TGachaCollection8 entity, PostUpdateContext<TGachaCollection8> context) {
    }

    @Override
    public void postDelete(TGachaCollection8 entity, PostDeleteContext<TGachaCollection8> context) {
    }
}