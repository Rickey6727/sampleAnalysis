package com.example.sample.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TNewMarkState75Listener implements EntityListener<TNewMarkState75> {

    @Override
    public void preInsert(TNewMarkState75 entity, PreInsertContext<TNewMarkState75> context) {
    }

    @Override
    public void preUpdate(TNewMarkState75 entity, PreUpdateContext<TNewMarkState75> context) {
    }

    @Override
    public void preDelete(TNewMarkState75 entity, PreDeleteContext<TNewMarkState75> context) {
    }

    @Override
    public void postInsert(TNewMarkState75 entity, PostInsertContext<TNewMarkState75> context) {
    }

    @Override
    public void postUpdate(TNewMarkState75 entity, PostUpdateContext<TNewMarkState75> context) {
    }

    @Override
    public void postDelete(TNewMarkState75 entity, PostDeleteContext<TNewMarkState75> context) {
    }
}