package com.example.sample.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TBlockAccount32Listener implements EntityListener<TBlockAccount32> {

    @Override
    public void preInsert(TBlockAccount32 entity, PreInsertContext<TBlockAccount32> context) {
    }

    @Override
    public void preUpdate(TBlockAccount32 entity, PreUpdateContext<TBlockAccount32> context) {
    }

    @Override
    public void preDelete(TBlockAccount32 entity, PreDeleteContext<TBlockAccount32> context) {
    }

    @Override
    public void postInsert(TBlockAccount32 entity, PostInsertContext<TBlockAccount32> context) {
    }

    @Override
    public void postUpdate(TBlockAccount32 entity, PostUpdateContext<TBlockAccount32> context) {
    }

    @Override
    public void postDelete(TBlockAccount32 entity, PostDeleteContext<TBlockAccount32> context) {
    }
}