package com.example.sample.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TBlockAccount64Listener implements EntityListener<TBlockAccount64> {

    @Override
    public void preInsert(TBlockAccount64 entity, PreInsertContext<TBlockAccount64> context) {
    }

    @Override
    public void preUpdate(TBlockAccount64 entity, PreUpdateContext<TBlockAccount64> context) {
    }

    @Override
    public void preDelete(TBlockAccount64 entity, PreDeleteContext<TBlockAccount64> context) {
    }

    @Override
    public void postInsert(TBlockAccount64 entity, PostInsertContext<TBlockAccount64> context) {
    }

    @Override
    public void postUpdate(TBlockAccount64 entity, PostUpdateContext<TBlockAccount64> context) {
    }

    @Override
    public void postDelete(TBlockAccount64 entity, PostDeleteContext<TBlockAccount64> context) {
    }
}