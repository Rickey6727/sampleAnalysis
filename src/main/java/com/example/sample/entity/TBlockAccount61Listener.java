package com.example.sample.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TBlockAccount61Listener implements EntityListener<TBlockAccount61> {

    @Override
    public void preInsert(TBlockAccount61 entity, PreInsertContext<TBlockAccount61> context) {
    }

    @Override
    public void preUpdate(TBlockAccount61 entity, PreUpdateContext<TBlockAccount61> context) {
    }

    @Override
    public void preDelete(TBlockAccount61 entity, PreDeleteContext<TBlockAccount61> context) {
    }

    @Override
    public void postInsert(TBlockAccount61 entity, PostInsertContext<TBlockAccount61> context) {
    }

    @Override
    public void postUpdate(TBlockAccount61 entity, PostUpdateContext<TBlockAccount61> context) {
    }

    @Override
    public void postDelete(TBlockAccount61 entity, PostDeleteContext<TBlockAccount61> context) {
    }
}