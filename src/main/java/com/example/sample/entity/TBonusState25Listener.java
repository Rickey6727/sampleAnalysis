package com.example.sample.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TBonusState25Listener implements EntityListener<TBonusState25> {

    @Override
    public void preInsert(TBonusState25 entity, PreInsertContext<TBonusState25> context) {
    }

    @Override
    public void preUpdate(TBonusState25 entity, PreUpdateContext<TBonusState25> context) {
    }

    @Override
    public void preDelete(TBonusState25 entity, PreDeleteContext<TBonusState25> context) {
    }

    @Override
    public void postInsert(TBonusState25 entity, PostInsertContext<TBonusState25> context) {
    }

    @Override
    public void postUpdate(TBonusState25 entity, PostUpdateContext<TBonusState25> context) {
    }

    @Override
    public void postDelete(TBonusState25 entity, PostDeleteContext<TBonusState25> context) {
    }
}