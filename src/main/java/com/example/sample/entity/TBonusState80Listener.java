package com.example.sample.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TBonusState80Listener implements EntityListener<TBonusState80> {

    @Override
    public void preInsert(TBonusState80 entity, PreInsertContext<TBonusState80> context) {
    }

    @Override
    public void preUpdate(TBonusState80 entity, PreUpdateContext<TBonusState80> context) {
    }

    @Override
    public void preDelete(TBonusState80 entity, PreDeleteContext<TBonusState80> context) {
    }

    @Override
    public void postInsert(TBonusState80 entity, PostInsertContext<TBonusState80> context) {
    }

    @Override
    public void postUpdate(TBonusState80 entity, PostUpdateContext<TBonusState80> context) {
    }

    @Override
    public void postDelete(TBonusState80 entity, PostDeleteContext<TBonusState80> context) {
    }
}