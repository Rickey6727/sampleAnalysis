package com.example.sample.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TBlockAccount82Listener implements EntityListener<TBlockAccount82> {

    @Override
    public void preInsert(TBlockAccount82 entity, PreInsertContext<TBlockAccount82> context) {
    }

    @Override
    public void preUpdate(TBlockAccount82 entity, PreUpdateContext<TBlockAccount82> context) {
    }

    @Override
    public void preDelete(TBlockAccount82 entity, PreDeleteContext<TBlockAccount82> context) {
    }

    @Override
    public void postInsert(TBlockAccount82 entity, PostInsertContext<TBlockAccount82> context) {
    }

    @Override
    public void postUpdate(TBlockAccount82 entity, PostUpdateContext<TBlockAccount82> context) {
    }

    @Override
    public void postDelete(TBlockAccount82 entity, PostDeleteContext<TBlockAccount82> context) {
    }
}