package com.example.sample.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TBlockAccount12Listener implements EntityListener<TBlockAccount12> {

    @Override
    public void preInsert(TBlockAccount12 entity, PreInsertContext<TBlockAccount12> context) {
    }

    @Override
    public void preUpdate(TBlockAccount12 entity, PreUpdateContext<TBlockAccount12> context) {
    }

    @Override
    public void preDelete(TBlockAccount12 entity, PreDeleteContext<TBlockAccount12> context) {
    }

    @Override
    public void postInsert(TBlockAccount12 entity, PostInsertContext<TBlockAccount12> context) {
    }

    @Override
    public void postUpdate(TBlockAccount12 entity, PostUpdateContext<TBlockAccount12> context) {
    }

    @Override
    public void postDelete(TBlockAccount12 entity, PostDeleteContext<TBlockAccount12> context) {
    }
}