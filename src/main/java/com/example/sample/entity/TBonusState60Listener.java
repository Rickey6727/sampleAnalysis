package com.example.sample.entity;

import org.seasar.doma.jdbc.entity.EntityListener;
import org.seasar.doma.jdbc.entity.PostDeleteContext;
import org.seasar.doma.jdbc.entity.PostInsertContext;
import org.seasar.doma.jdbc.entity.PostUpdateContext;
import org.seasar.doma.jdbc.entity.PreDeleteContext;
import org.seasar.doma.jdbc.entity.PreInsertContext;
import org.seasar.doma.jdbc.entity.PreUpdateContext;

/**
 * 
 */
public class TBonusState60Listener implements EntityListener<TBonusState60> {

    @Override
    public void preInsert(TBonusState60 entity, PreInsertContext<TBonusState60> context) {
    }

    @Override
    public void preUpdate(TBonusState60 entity, PreUpdateContext<TBonusState60> context) {
    }

    @Override
    public void preDelete(TBonusState60 entity, PreDeleteContext<TBonusState60> context) {
    }

    @Override
    public void postInsert(TBonusState60 entity, PostInsertContext<TBonusState60> context) {
    }

    @Override
    public void postUpdate(TBonusState60 entity, PostUpdateContext<TBonusState60> context) {
    }

    @Override
    public void postDelete(TBonusState60 entity, PostDeleteContext<TBonusState60> context) {
    }
}